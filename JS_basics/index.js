console.log('hello world');
let firstName = 'saksham', lastName = 'garg';
console.log(firstName ,lastName);
const intrestRate = 0.3;
// intrestRate = 1; // can't change a variable
console.log(intrestRate);

// premitive types 
let name = 'saksham';// string literal
let age = 30;// number literal
let isApproved = false;//boolean literal
let fname = undefined;
let selectColor = null;// // null literal
// js is a dynamic language we can change it's type during run time.
name = 1;
console.log(typeof name);
typeof name;